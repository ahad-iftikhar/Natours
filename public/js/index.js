import '@babel/polyfill';
import { login } from './login';
import { displayMap } from './mapbox';

// Implemented this file but the web was not working, login functionality etc so put back the xode in thier files

// DOM ELEMENTS
// const loginForm = document.querySelector('.form');
// const mapBox = document.getElementById('map');

// if (mapBox) {
// const locations = JSON.parse(mapBox.dataset.locations); // Getting locations data from pug template, we have created a string of all the locations in pug template and set it into a dataset and we are accessing it here.
// displayMap(locations);
// }

// if (loginForm)
// loginForm.addEventListener('submit', (e) => {
//   e.preventDefault();
//   const email = document.getElementById('email').value;
//   const password = document.getElementById('password').value;
//   login(email, password);
// });
